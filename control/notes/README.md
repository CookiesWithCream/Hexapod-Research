# Control Notes

This folder is intentionally kept minimal.

Older personal notes, future-task reminders, and copied terminal logs were removed from the cleaned package. Current working information is documented in:

- `../README.md` for current control scripts and commands
- `../../README.md` for project-level structure
- `../../PROJECT_STRUCTURE.md` for folder organization

Use this folder only for future clean technical notes that describe tested project behavior, not personal reminders or temporary task planning.
