# Control Legacy

This folder stores the preserved web-only controller reference.

## File

- `WebLegacy_SControlX2Web.py` — older web-focused controller kept as a fallback/reference before the combined terminal + web workflow became the main path.

Use `../SControl.py` or `../IKControl.py` for current work.
